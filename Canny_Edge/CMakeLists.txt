cmake_minimum_required(VERSION 3.0)
project(canny)
find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCDLUE_DIRS})
add_executable(${PROJECT_NAME} main.cpp)
target_link_libraries(${PROJECT_NAME} ${OpenCV_LIBS})