cmake_minimum_required(VERSION 3.0)
project(opening)
find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
add_compile_options("-g")
add_executable(opening main.cpp)
target_link_libraries(opening ${OpenCV_LIBS})
